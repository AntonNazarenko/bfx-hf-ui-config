const { BFX_HF_CUSTOM } = require('./charts')
const { DARK } = require('./themes')
const { EN } = require('./languages')

module.exports = {
  affiliateCode: 'xZvWHMNR',
  dms: false,
  ga: true,
  chart: BFX_HF_CUSTOM,
  theme: DARK,
  language: EN
} 
